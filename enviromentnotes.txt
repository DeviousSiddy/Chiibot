python3.9 pip install virtualenv (if you don't already have virtualenv installed)
python3.9 virtualenv venv to create your new environment (called 'venv' here)
python3.9 source venv/bin/activate to enter the virtual environment
python3.9 pip install -r requirements.txt to install the requirements in the current environment